services:
  ats-cv-tailor:
    build: .
    container_name: ats-cv-tailor
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./config.ini:/app/config.ini
    environment:
      - DISABLE_HMR=true
    restart: unless-stopped
