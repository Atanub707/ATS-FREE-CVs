version: '3.8'

services:
  ats-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=3000
    volumes:
      - ./data:/app/data
      - ./config.ini:/app/config.ini
    restart: unless-stopped
